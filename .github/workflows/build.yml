name: build
on:
  workflow_run:
      workflows: ["CI release"]
      types:
        - completed

jobs:
  build:
      name: build
      runs-on: ubuntu-latest
      defaults:
        run:
          working-directory: src
      steps:
        - uses: actions/checkout@v2
        - name: make
          run: make
